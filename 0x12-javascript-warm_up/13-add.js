#!/usr/bin/node
module.exports = {
  /**
   * add - Function that computes the sum of 2 numbers.
   * @param {Number} a - 1st number
   * @param {Number} b - 2nd  number
   * Return:  The sum of the 2 numbers.
   */
  add: (a, b) => a + b
};
