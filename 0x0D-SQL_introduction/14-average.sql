-- Script that computers score average of all records in
-- 'second_table' of 'hbtn_0c_0'
-- The result column name should be 'average'
-- Database name will be passed as arg of mysql cmd
SELECT AVG(score) AS average FROM second_table;
